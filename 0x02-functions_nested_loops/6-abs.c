#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @j: the parameter passed
 * Return: j value
 *
 */
int _abs(int j)
{
	if (j < 0)
	{
		j = j * -1;
		return (j);
	}
	else
	{
		return (j);
	}
}
